# Виправлення підрахунку звернень на Dashboard

**Дата:** 29 жовтня 2025  
**Проблема:** Неправильна кількість звернень на сторінці http://localhost:3000/dashboard

## 🐛 Виявлена проблема

На дашборді адміністратора відображалася **неправильна кількість звернень** через помилку в SQL-запитах в backend.

### Причина

У файлі `api/app/crud.py` в функціях для dashboard analytics використовувався **неправильний підхід з вкладеними subquery**, що призводило до некоректного підрахунку:

```python
# ❌ НЕПРАВИЛЬНО (старий код)
total_cases = db.execute(
    select(func.count(models.Case.id)).select_from(query.subquery())
).scalar() or 0
```

Проблема полягала в тому, що створювався SELECT запит (`query = select(models.Case)`), а потім з нього робився subquery, і з цього subquery знову робився COUNT. Це призводило до неправильної логіки підрахунку.

## ✅ Виправлення

### 1. Функція `get_dashboard_summary()` (рядок 1983)

**Було:**
- Використання вкладених subquery для підрахунку
- Неправильна логіка фільтрації

**Стало:**
- Прямий COUNT без зайвих вкладень
- Правильне застосування фільтрів через масив умов

```python
# ✅ ПРАВИЛЬНО (новий код)
base_filter = []

if date_from:
    date_from_dt = datetime.fromisoformat(date_from.replace('Z', '+00:00'))
    base_filter.append(models.Case.created_at >= date_from_dt)

if date_to:
    date_to_dt = datetime.fromisoformat(date_to.replace('Z', '+00:00'))
    base_filter.append(models.Case.created_at <= date_to_dt)

# Прямий підрахунок
total_query = select(func.count(models.Case.id))
if base_filter:
    total_query = total_query.where(*base_filter)

total_cases = db.execute(total_query).scalar() or 0
```

### 2. Функція `get_status_distribution()` (рядок 2045)

**Виправлено аналогічну проблему:**
- Видалено вкладені subquery
- Застосовано той самий підхід з `base_filter`

### 3. Функція `get_top_categories()` (рядок 2281)

**Найбільше виправлень:**
- Загальний підрахунок звернень
- GROUP BY для категорій
- Підрахунок по статусах для кожної категорії

**Було:**
```python
category_query = query.where(models.Case.category_id == category_id)

new_count = db.execute(
    select(func.count(models.Case.id))
    .select_from(category_query.where(models.Case.status == models.CaseStatus.NEW).subquery())
).scalar() or 0
```

**Стало:**
```python
new_query = select(func.count(models.Case.id)).where(
    models.Case.category_id == category_id,
    models.Case.status == models.CaseStatus.NEW
)
if base_filter:
    new_query = new_query.where(*base_filter)
new_count = db.execute(new_query).scalar() or 0
```

## 🧪 Результати тестування

### Перевірка в БД

**Пряме підключення до PostgreSQL:**
```sql
SELECT status, COUNT(*) as count FROM cases GROUP BY status ORDER BY status;

   status    | count 
-------------+-------
 NEW         |    14
 IN_PROGRESS |     3
 REJECTED    |     1
 DONE        |     1
(4 rows)

SELECT COUNT(*) as total_cases FROM cases;
 total_cases: 19
```

### API Endpoints (після виправлення)

**1. Dashboard Summary** (`GET /api/dashboard/summary`)
```json
{
    "total_cases": 19,
    "new_cases": 14,
    "in_progress_cases": 3,
    "needs_info_cases": 0,
    "rejected_cases": 1,
    "done_cases": 1
}
```
✓ **Перевірка:** 14 + 3 + 0 + 1 + 1 = 19 ✓  
✓ **Співпадає з БД:** 19 ✓

**2. Status Distribution** (`GET /api/dashboard/status-distribution`)
```json
{
    "total_cases": 19,
    "distribution": [
        { "status": "NEW", "count": 14, "percentage": 73.68 },
        { "status": "IN_PROGRESS", "count": 3, "percentage": 15.79 },
        { "status": "NEEDS_INFO", "count": 0, "percentage": 0.0 },
        { "status": "REJECTED", "count": 1, "percentage": 5.26 },
        { "status": "DONE", "count": 1, "percentage": 5.26 }
    ]
}
```
✓ **Перевірка:** Сума відсотків = 100% ✓  
✓ **Співпадає з БД:** Кількість по статусах точно співпадає ✓

## 📊 Вплив на Frontend

Після виправлення на сторінці **http://localhost:3000/dashboard** тепер відображаються **правильні дані:**

### Віджети з правильною статистикою:
- ✅ **Всього звернень:** 19 (було 361 - помилка!)
- ✅ **Нові:** 14 (було 266 - помилка!)
- ✅ **В роботі:** 3 (було 57 - помилка!)
- ✅ **Потребують інформації:** 0
- ✅ **Завершено:** 1 (було 19 - помилка!)

### Графіки:
- ✅ **Розподіл по статусах** - правильні відсотки (73.68% NEW, 15.79% IN_PROGRESS)
- ✅ **ТОП категорій** - правильна кількість та відсотки

**Важливо:** Тепер дані на dashboard **повністю співпадають** з даними в базі даних PostgreSQL!

## 🔧 Технічні деталі

### Змінені файли:
- `ohmatdyt-crm/api/app/crud.py`

### Функції що виправлено:
1. `get_dashboard_summary()` - рядок 1983
2. `get_status_distribution()` - рядок 2045
3. `get_top_categories()` - рядок 2281

### Підхід до виправлення:
```python
# Замість створення складних вкладених запитів:
query = select(models.Case).where(filters)
count = db.execute(select(func.count()).select_from(query.subquery())).scalar()

# Використовуємо простий та зрозумілий підхід:
base_filter = [умова1, умова2, ...]
count_query = select(func.count(models.Case.id))
if base_filter:
    count_query = count_query.where(*base_filter)
count = db.execute(count_query).scalar() or 0
```

## ✨ Переваги нового підходу

1. **Простіша логіка** - легше читати та підтримувати код
2. **Кращі SQL запити** - менше вкладеності, ефективніше виконання
3. **Правильні результати** - коректний підрахунок звернень
4. **Уніфікований підхід** - однаковий спосіб фільтрації у всіх функціях
5. **Легше дебажити** - зрозуміліша структура запитів

## 🚀 Deployment

Для застосування виправлень потрібно **пересобрати API контейнер**, оскільки код змінено:

```bash
cd ohmatdyt-crm

# Пересборка API з новим кодом
docker-compose build api

# Перезапуск API контейнера
docker-compose up -d api
```

**Чому потрібна пересборка?**
- API контейнер не має volume mount для коду
- Код копіюється в image при build
- Простий `restart` не оновить код

Або повна пересборка всіх сервісів (якщо потрібно):
```bash
docker-compose down
docker-compose up -d --build
```

## ✅ Статус

- [x] Проблема виявлена
- [x] Код виправлено
- [x] API перезапущено
- [x] Тестування пройдено успішно
- [x] Frontend відображає правильні дані

---

**Виконав:** GitHub Copilot  
**Підтверджено:** Всі 3 функції dashboard analytics працюють коректно  
**Версія API:** Ohmatdyt CRM v1.0
